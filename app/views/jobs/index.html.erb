<%= render partial: "shared/page_header", locals: { title: '求人情報一覧' } %>
<div class="mt50">
  <div class="container clearfix">
      <div class="row clearfix">
          <div class="col-lg-12">
            <%= render partial: "job_item", locals: {jobs: @jobs} %>
          </div>
      </div>
      <div class="row">
          <div class="col-lg-12 mt30">
              <%= paginate @jobs %>
          </div>
      </div>
  </div>
 </div>
